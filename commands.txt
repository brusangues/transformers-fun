pip install uv
uv pip install torch torchvision torchaudio -U --index-url https://download.pytorch.org/whl/cu128
uv pip install torch torchvision matplotlib tensorboard
uv pip install -r requirements.txt

pip install gpustat
gpustat -cp -i

python train.py v5

tensorboard --logdir=models

pip freeze | grep -E "unidecode|tqdm|torch|torchvision|torchaudio|pyyaml|jupyter|pandas|tiktoken|sentencepiece|joblib|mobi|ebooklib|BeautifulSoup4|openpyxl|pyarrow"

kaggle kernels output brusangues/transformers-fun/output -p models/kaggle/
